<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title : 'Embed Insta' %></title>
  <link rel="stylesheet" href="/static/css/style.css" />
</head>
<body>
  <header class="site-header">
    <div class="container">
      <a class="brand" href="/">Embed Insta</a>
      <nav>
        <a href="/">Feed</a>
        <a href="/api/instagram/posts" target="_blank">API</a>
        <% if (typeof admin !== 'undefined' && admin) { %>
          <a href="/admin">Admin</a>
        <% } else { %>
          <a href="/admin/login">Admin</a>
        <% } %>
      </nav>
    </div>
  </header>
  <main class="container">
